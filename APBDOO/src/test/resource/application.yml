spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql:~/test;AUTO_SERVER=TRUE
  jpa:
    hibernate.ddl-auto: validate
    properties.hibernate:
      jdbc.lob.non_contextual_creation: true

  flyway.enabled: true